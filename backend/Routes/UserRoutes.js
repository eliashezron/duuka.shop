import express from 'express'
const router = express.Router()
import {registerUser, getAllUsers, loginUser, getUserProfile, deleteUser, updateUserProfile} from '../Controllers/UserController'

router.get(
    
)
router.post(

)
router.put(

)
router.delete(

)